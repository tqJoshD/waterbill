
import tkinter
import random

#This is just for reference when the inputted cubic meter is converted into peso
#Converted_Cubic_Meter - is a variable once cubic meter is converted to peso

def calculating_the_total_amount():
    Converted_Cubic_Meter = Totalcost
    VAT = Converted_Cubic_Meter * 0.12
    Arrears = float(Arrears_Entry.get())
    Total_Amount = VAT + Arrears + Converted_Cubic_Meter


#Generating WIN no.
WIN = random.randint(100000, 999999)
win_label.config(text=f"WIN Number: {WIN}")
